public class AlphabetCheck {
    public static void main(String[] args) {
        char ch = '10'; // Character to check

        if (Character.isLetter(ch)) {
            System.out.println(ch + " is an alphabet.");
        } else {
            System.out.println(ch + " is not an alphabet.");
        }
    }
}
